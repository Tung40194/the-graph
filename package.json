{
  "name": "subgraph-deployment",
  "version": "1.0.0",
  "description": "to manage subgraph-deployment",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "gen-graph-manifest": "npx graph-compiler --config subgbraph-config.json --include node_modules/@openzeppelin/subgraphs/src/datasources --export-schema --export-subgraph"
  },
  "repository": {
    "type": "git",
    "url": "git@git.baikal.io:tungpham/subgraph-deployment.git"
  },
  "author": "tungpham",
  "license": "ISC",
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.29.0",
    "@openzeppelin/subgraphs": "^0.1.7-1",
    "mustache": "^4.2.0"
  }
}
